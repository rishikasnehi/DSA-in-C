#include <stdio.h>
int main()
{
    
    char str[50];
    printf("Enter a string : ");
    scanf("%[^\n]",str);
    int length = 0, i = 0;
    while(str[i] != '\0')
    {
        length++;
        i++;
    }
    printf("%d",length);
	return 0;
}
